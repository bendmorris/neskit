include "SDL2/SDL.h";

trait ScreenBuffer {
    public function drawScreen(pixels: Ptr[Uint32]): Void {

    }
}

implement ScreenBuffer for SDL_Texture {
    public function drawScreen(pixels: Ptr[Uint32]) {
        SDL_UpdateTexture(&this, null, pixels, 256 * 240 * (sizeof Uint32));
    }
}
